~/.gemini/GEMINI.md

Never run `git add`, `git commit`, or modify the Git index unless I explicitly request it.

Do not stage any files automatically.

Only read or modify files that I explicitly name in my prompt.

Ask for confirmation before executing any terminal command that changes files or Git state.

1. Problem Understanding
- One-sentence restatement of the problem in your own words
- Clarify what is being asked (transform, search, optimize, etc.)

2. Inputs / Outputs
- Input: data type + structure (e.g., arr: List[int])
- Output: what to return OR confirm in-place modification
- Clarify side effects (in-place vs new structure)

3. Constraints & Assumptions
- Input size limits (or reasonable assumptions if not given)
- Data type constraints (ints, strings, etc.)
- Memory constraints (if relevant)
- Assumptions (sorted? unique? value range?)
- In-place requirement / stability requirement (if applicable)

4. Approach
- High-level intuition (why this works)
- Step-by-step algorithm
- Key idea / pattern (two pointers, BFS, DP, sorting, etc.)
- Optional correctness reasoning / invariant

5. Edge Cases
- Empty input: [], null
- Single element
- Already optimal input (best case)
- Worst-case input pattern
- Duplicates / repeated values
- Large input / performance stress cases

6. Complexity Analysis

Time Complexity:
- Best case:
- Average case:
- Worst case:

Space Complexity:
- Auxiliary space: 
- Extra data structures used: 

7. Implementation
```code
```